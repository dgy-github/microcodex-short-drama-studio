# Handoff：真实媒体质量评估

## 目标

为生图和生视频 Agent 建立版本化、可审计、可插拔的质量门禁，区分故事对齐、人物一致性、构图/动作、连续性和伪影，并输出确定性返工阶段。

## 范围

只修改两个 Agent 的 quality 模块、质量契约和测试；Python 不得调用网络、文件系统、shell 或 provider。

## 依赖与验收

无前置依赖。覆盖缺失、越界、NaN、模型异常、版本漂移；质量失败不得创建精生成请求。分别运行两个 Agent 的 unittest。

## 状态

`pending`。完成后记录两个仓库提交号、测试结果和指标依据。
