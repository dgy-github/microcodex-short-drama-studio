# Handoff：生视频 Agent 发布收口

## 目标

整合 Wan、可灵和质量流水线成果，完成 story-video-agent 的独立版本、契约示例、README、CI 和 GitHub 发布验收。

## 依赖与范围

依赖任务 01、02、03。只修改 `D:/github_dgy/story-video-agent/` 的发布元数据、文档、契约和测试；不得修改主项目或生图 Agent。

## 验收

测试和 GitHub Actions 全绿；无 secret、受保护素材或 `*.egg-info`。完成后记录版本、仓库 URL、提交号和 CI URL。

## 状态

`pending`。
