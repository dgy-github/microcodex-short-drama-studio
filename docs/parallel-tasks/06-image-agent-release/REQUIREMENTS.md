# 任务 06：生图 Agent 发布收口

## 目标

整合质量任务成果，完成 `story-image-agent` 的独立版本、契约示例、CI、README 和 GitHub 发布验收。

## 依赖

任务 03 完成后开始。

## 写入范围

仅 `D:/github_dgy/story-image-agent/` 的发布元数据、文档、契约和测试。

## 验收

测试与 GitHub Actions 全绿；仓库不包含 secret、真实受保护素材或 `*.egg-info`。
