# Code standards

- 函数目标 40 逻辑行，60 行开始提取，超过 80 行需说明。
- 生产文件目标 400 行，500 行拆分，超过 700 行需例外记录。
- Svelte 组件目标不超过 300 行；嵌套不超过三层。
- 验证、策略、纯计算、持久化、供应商 I/O 和进程 I/O 保持可分离。
- 新增逻辑前先查 `PROJECT_REGISTRY.yaml`；同一授权、状态机、重试、
  预算、映射或错误规则不得出现第二所有者。
- 注释解释安全边界、不变量、并发、重试、兼容和破坏性行为的原因。
- 公共接口说明用途、输入、输出、错误、副作用和复用约束。
